{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Incapacidad</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Fechainicial</th>
                <td>{{ entity.fechaInicial|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Fechafinal</th>
                <td>{{ entity.fechaFinal|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Notas</th>
                <td>{{ entity.notas }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_incapacidad') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_incapacidad_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
