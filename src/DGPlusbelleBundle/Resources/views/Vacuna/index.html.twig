{% extends 'DGPlusbelleBundle:Layout:layouterpajustes.html.twig' %}

{% block contenido -%}
    
    
    
    <div class="content-body">
          <div class="panel" data-fill-color="true">
            <div class="panel-heading">
              <div class="panel-control pull-right">
                {#<a href="#" class="btn btn-icon" data-toggle="panel-refresh" rel="tooltip" data-placement="bottom" title="refresh"><i class="icon-refresh"></i></a>
                <a href="#" class="btn btn-icon" data-toggle="panel-expand" rel="tooltip" data-placement="bottom" title="expand"><i class="arrow_expand"></i></a>
                <a href="#" class="btn btn-icon" data-toggle="panel-collapse" rel="tooltip" data-placement="bottom" title="collapse"><i class="icon_minus_alt2"></i></a>#}
              </div>
                <strong><h3 class="panel-title"><strong>Vacunas</strong></h3></strong>
            </div><!-- /.panel-heading -->

            <div class="panel-body">
              <div class="btn-toolbar clearfix" role="toolbar">
               
                <div class="btn-group btn-group-sm pull-left">
                    <button style="height:30px;" data-toggle="tooltip" data-container="body" title="Nuevo paciente" id="add-datatables1" class="btn btn-default" role="button">
                        <i class="fa fa-plus" aria-hidden="true"></i>
                    </button> 
                    {#<button data-toggle="tooltip" data-container="body" title="Detalles paciente" id="detalle-datatables1" class="btn btn-default disabled" role="button">
                        <i class="fa fa-bars"></i>
                    </button> #}
                </div>
                {#<div class="btn-group btn-group-sm pull-left">
                    <button id="ver-expediente" data-toggle="tooltip" data-container="body" title="Ver expediente" class="btn btn-default datatables1-actions disabled opciones" role="button" style="display:none;">
                        <i class="fa fa-book"></i><a href="{{ path('admin_historial_consulta') }}"> </a>
                    </button> 
                </div>#}
                <div class="btn-group btn-group-sm pull-left">
                    
                    {#<button id="nueva-cita" data-toggle="tooltip" data-container="body" title="Nueva cita" class="btn btn-default datatables1-actions disabled opciones" role="button">
                        <i class="fa fa-calendar-plus-o"></i><a href="{{ path('admin_cita_new') }}"> </a>
                    </button>#}
                    
                    {#<button id="nueva-consulta" data-toggle="tooltip" data-container="body" title="Nueva consulta" class="btn btn-default datatables1-actions disabled opciones" role="button">
                        <i class="fa fa-folder-open"></i><a href="{{ path('admin_consulta_nueva_paciente') }}"> </a>
                    </button>#}
                    {#<button id="nueva-consulta" data-toggle="tooltip" data-container="body" title="Nueva consulta emergencia" class="btn btn-default datatables1-actions disabled opciones" role="button">
                        <i class="fa fa-folder-open"></i><a href="{{ path('admin_consulta_nueva_paciente') }}"> </a>
                    </button>#}
                   
                    {#<button id="modificar-vacuna" data-toggle="tooltip" data-container="body" title="Modificar vacuna" class="btn btn-default datatables1-actions disabled opciones" role="button" style="display:none;">
                        <i class="fa fa-leanpub"></i><a href="{{ path('admin_vacuna_edit') }}"> </a>
                    </button> #}
                    {#<button id="vender-paquete" data-toggle="tooltip" data-container="body" title="Vender paquete" class="btn btn-default datatables1-actions disabled opciones" role="button" style="display:none;">
                        <i class="fa fa-briefcase"></i><a href="{{ path('admin_ventapaquete_new') }}"> </a>
                    </button> #}
                    {#<button id="vender-tratamiento" data-toggle="tooltip" data-container="body" title="Vender tratamiento" class="btn btn-default datatables1-actions disabled opciones" role="button" style="display:none;">
                        <i class="fa fa-leanpub"></i><a href="{{ path('admin_personatratamiento_new') }}"> </a>
                    </button> 
                     <button id="nuevo-abono" data-toggle="tooltip" data-container="body" title="Nuevo abono" class="btn btn-default datatables1-actions disabled opciones" role="button" style="display:none;">
                        <i class="fa fa-dollar"></i><a href="{{ path('admin_abono_new') }}"> </a>
                    </button> #}
                    
                </div>
                <div class="btn-group btn-group-sm pull-left">
                    <button style="height:30px;" id="edit-datatables1" data-toggle="tooltip" data-container="body" title="Modificar vacuna" class="btn btn-default datatables1-actions disabled" role="button">
                        <i class="fa fa-edit"></i>
                    </button>
                    <button style="height: 30px;" id="delete-datatables1" data-toggle="tooltip" data-container="body" title="Cambiar estado" class="btn btn-default datatables1-actions disabled" role="button">
                        <i class="fa fa-toggle-off"></i>
                    </button>
                  
                </div>
                <div class="btn-group pull-left">
                  <input id="filterDatatables1" class="form-control input-sm" placeholder="Buscar...">
                </div>
              </div><!-- /btn-toolbar -->
            </div><!-- /.panel-body -->

            <div class="panel-body hide" id="addFormContainer">
              {% include 'DGPlusbelleBundle:Vacuna:new.html.twig' %}
            </div><!-- /.panel-body -->
            
            <div class="panel-body hide" id="editFormContainer"></div>
            <div class="panel-body hide" id="opcionesFormContainer"></div>

       
     {#<a href="{{ path('admin_paciente_new') }}" class="btn btn-success"> Crear nuevo paciente</a>  #}

    <table id = "datatables1" class="table table-bordered table-hover display dt-responsive" style="margin-top: 30px">
        <thead>
            <tr>
                {#<th>Id</th>#}
                <th>Nombre</th>
                <th>Estado</th>
                {#<th>Actions</th>#}
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr id="{{entity.id}}">
                {#<td><a href="{{ path('admin_vacuna_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>#}
                <td>{{ entity.nombre }}</td>
                <td><center>
                    {% if entity.estado == 1 %}
                        Activo
                    {% else %}
                        Inactivo
                    {% endif %}
                    
                </center></td>
                {#<td>
                <ul>
                    <li>
                        <a href="{{ path('admin_vacuna_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_vacuna_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>#}
            </tr>
        {% endfor %}
        </tbody>
    </table>
    </div><!-- /.panel -->    
       
<!-- Template Setups -->
     {#   <div class="modal fade" id="templateSetup">
          <div class="modal-dialog">
            <!-- modal-content -->
            <div class="modal-content"></div>
          </div><!-- /.modal-dialog -->
        </div><!-- /.templateSetup -->
            #}
      </div><!-- /.content -->
    
    
    {% endblock %}

    {% block javascripts_step %}
        {#<script src="{{ asset('Resources/alertifyjs/alertify.js') }}"></script>#}
        {#<script src="{{ asset('Resources/wrapkit/scripts/jquery.mask.js') }}"></script>#}
        <script src="{{ asset('Resources/Zebra/js/zebra_datepicker.js') }}"></script>
        {#<script src="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.2/js/select2.min.js"></script>#}
        <script src="{{ asset('Resources/wrapkit/scripts/dataTables.bootstrap.js') }}"></script>
        <script src="{{ asset('Resources/wrapkit/scripts/dataTables.tableTools.js') }}"></script>
        {#<script type="text/javascript" src="{{ asset('Resources/bValidator-0.73/jquery.bvalidator.js')}}"></script>
        <script type="text/javascript" src="{{ asset('Resources/bValidator-0.73/bvalidator.lang.es.js')}}" ></script>#}
        <script src="{{ asset('Resources/wrapkit/scripts/bootbox.js') }}"></script>
        <script src="{{ asset('Resources/js/Empleado.js') }}"></script>
      
       <script>
            $(document).ready(function(){
                 var link=0;
                 var estadoPrevio = "";
                 {#$('#dgplusbellebundle_paciente_fechaNacimiento').daterangepicker({
                    singleDatePicker: true,
                    showDropdowns: true,
                    locale: {
                        format: 'YYYY-MM-DD'
                    }
                }); #}
                
                // Validando campo telefono
                 
                
                //Seleccionar un registro
                $('#datatables1>tbody>tr').on('click', function(event) {
                    //Se limpia los otros que esten activos
                    $('#datatables1>tbody>tr').each(function(index, val) {
                        $(this).removeClass("active");
                    });
                    //Se activa el registro en el que se hizo click
                    $(this).addClass("active");
                    //Se habilitan los botones de editar y borrar
                    $("#edit-datatables1").removeClass("disabled");
                    $("#delete-datatables1").removeClass("disabled");
                    
                    //var link=$('#datatables1>tbody>tr>td>a').attr("href");
                    //var link=$(this).children("td:first").children().attr("href");
                    //Obtener el id del registro
                    link=$(this).attr("id");
                    //alert(link);
                    //Carga del form modificar
                    $("#editFormContainer").load(link+"/edit");
                    
                    
                     //Hay que cambiar el numero de la columna para las otras tablas
                    estadoPrevio = $(this).children('td:nth-child(2)').children().html();
                    estadoPrevio = estadoPrevio.trim();
                    {#console.log(estadoPrevio);#}
                    if(estadoPrevio==="Inactivo"){//se muestran los iconos de desactivado
                        $('#delete-datatables1').children().removeClass("fa fa-toggle-on");
                        $('#delete-datatables1').children().addClass("fa fa-toggle-off");
                    }
                    else{//se muestran los iconos de activado
                        $('#delete-datatables1').children().removeClass("fa fa-toggle-off");
                        $('#delete-datatables1').children().addClass("fa fa-toggle-on");
                    }
                    //var href=link.attr("href");
                    //alert(object);
                    
                    
                });
                
                
                $("#add-datatables1").click(function(){
                    if($("#addFormContainer").hasClass("hide")){
                        $("#addFormContainer").removeClass("hide");
                        $("#editFormContainer").addClass("hide");
                    }
                    else{
                        $("#addFormContainer").addClass("hide");
                        limpiarCampos();
                        //$("#editFormContainer").removeClass("hide");
                    }

                });
                
                 $("#delete-datatables1").click(function(){
                     if(!$(this).hasClass('disabled')){                     
                     bootbox.confirm("¿Desea cambiar el estado de este registro?", function(result) {
                        if(result){
                        $.getJSON(Routing.generate('admin_vacuna_desactivar', { id: link}), 
                          function(data) {
                          var mensaje="";
                                    if(data.regs==0){
                                        //Se cambia a desactivado
                                        mensaje ="Vacuna desactivado con exito";
                                        $('#'+link + ' > td:nth-child(2)').html('Inactivo');
                                        $('#delete-datatables1').children().removeClass("fa fa-toggle-on");
                                        $('#delete-datatables1').children().addClass("fa fa-toggle-off");
                                    //$('#datatables1>tbody>tr>td:nth-child(5)').html('Inactivo');
                                    }
                                    if(data.regs==1){
                                        //Se cambia a activado
                                        mensaje ="Vacuna activada con exito";
                                        $('#'+link + ' > td:nth-child(2)').html('Activo');
                                        $('#delete-datatables1').children().removeClass("fa fa-toggle-off");
                                        $('#delete-datatables1').children().addClass("fa fa-toggle-on");
                                    }
                                    bootbox.alert(mensaje, function() {
                                        //Example.show("Hello world callback");
                                    });
                                    
                                // alert(link);
                            }); 
                        }
                                    
                    });     
                        } 

                }); 
                
                //Accion cancelar del boton nuevo
                $("#cancelarNuevo").click(function(){
                    $("#add-datatables1").click();
                    limpiarCampos();
                    return false;
                });
                
                //Accion cancelar del boton modificar
                $("#cancelarModificar").click(function(){
                    $("#edit-datatables1").click();
                    return false;
                });
                
                
                $("#edit-datatables1").click(function(){
                    if(!$("#edit-datatables1").hasClass("disabled")){
                        if($("#editFormContainer").hasClass("hide")){
                            $("#addFormContainer").addClass("hide");
                            $("#editFormContainer").removeClass("hide");
                        }
                        else{
                            //$("#addFormContainer").removeClass("hide");
                            $("#editFormContainer").addClass("hide");
                        }
                    }

                });
                /*
                $("#dgplusbellebundle_sucursal_submit").click(function(){
                    $(".icon-refresh").click();
                    return false;
                });
                */
                $('#filterDatatables1').on('keyup', function(event) {
                    var filtro = $(this).val(); 
                    //alert(filtro);
                    $('#datatables1_filter>label>input').val($(this).val());
                    $('#datatables1_filter>label>input').keyup();

                });
                
                
                $('#datatables1').DataTable({ 
                    dom:'ftp',
                    searching:true,
                    "order":[0, "desc"],
                    "columnDefs": [
                           { "orderable": false, "targets": 0 }{#,
                           { "orderable": false, "targets": 2 },
                           { "orderable": false, "targets": 3 }#}
                           
                           //{ "orderData": [ 2, 3, 4 ], "targets": 2 }
                    ],
                    "language": {
                    "info": "Mostrando página _PAGE_ de _PAGES_",
                    "infoEmpty": "",
                    "emptyTable": "No se encontraron registros",
                    "paginate": { 
                        "next": "Siguiente",
                        "previous": "Anterior"
                    },
                    "processing": "Procesando petición...",
                    "search": "Buscar registros:",
                    "lengthMenu": "Mostrar _MENU_ registros"
                }, 
                    
        });
        
        $('#datatables1>tr>td').on('click', function(event) {
            alert('dsv');
        });
        //Oculta el filtro de la tabla
        $('#datatables1_filter').hide();
        $('#filterDatatables1').val('');
        
        
{#        validarEmpleado(); #}
        
    });//Fin document ready
        
    //Definición de funciones
    function limpiarCampos(){
        //Limpiar los campos de tipo text
        $("input[type=text]").each(function(index, val) {
            $(this).val("");
        });
        //Seleccionar el primer elemento de los dropdown
        $('select option').each( function(index, val) {
            if(index == 0){
                $(this).attr("selected","selected");
            }
            else{
                $(this).removeAttr("selected");
            }
            
        
	});             
    };
        </script>

    {% endblock %} 
  
   

      
